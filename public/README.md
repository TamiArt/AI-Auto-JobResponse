# Public assets

Place the application icon at `public/icon.png`. The same file is used as the
browser favicon, Apple touch icon, installable PWA icon, and in-app brand mark.

For the best results, use a square PNG image with a resolution of at least
512×512 pixels and keep important artwork away from the outer 10% of the image.
