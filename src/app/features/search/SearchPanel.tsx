import { useState } from "react";
import { AnimatePresence, motion } from "motion/react";
import { AlertCircle, Briefcase, Building2, Check, Clock, ExternalLink, Loader2, MapPin, RefreshCw, Star, Target, Trash2 } from "lucide-react";
import type { Config, EmploymentTypeFilter, ExperienceFilter, JobSource, SalaryCurrency, WorkModeFilter } from "../../domain/types";
import { AREA_OPTIONS, JOB_SOURCES } from "../../data/catalog";
import { PROFESSION_EXAMPLES, PROFESSION_GROUPS } from "../../data/professions";
import { mergeSearchResults, searchJobs, type RealJobSource, type SearchRequest, type SearchResult, type SearchResponse, type SourceRefreshMeta } from "./searchService";
import { addSearchHistory, clearSearchHistory, loadFavorites, loadSearchHistory, persistFavorites, type SearchHistoryEntry } from "./searchStorage";

const USER_SOURCES: RealJobSource[] = ["hh", "arbeitnow"];
const LIMITED_REFRESH_SOURCES: RealJobSource[] = ["jobicy", "remotive"];
const EXTERNAL_SOURCES: JobSource[] = ["habr", "geekjob", "finder", "djinni", "remoteco", "linkedin", "indeed", "glassdoor", "wellfound", "behance", "dribbble", "artstation"];
const SALARY_CURRENCIES: Array<{ value: SalaryCurrency; label: string }> = [{ value: "RUB", label: "₽ RUB" }, { value: "USD", label: "$ USD" }, { value: "EUR", label: "€ EUR" }, { value: "GBP", label: "£ GBP" }];

const WORK_MODE_OPTIONS: Array<{ value: WorkModeFilter; label: string }> = [{ value: "any", label: "Любой формат" }, { value: "remote", label: "Удалённо" }, { value: "hybrid", label: "Гибрид" }, { value: "onsite", label: "Офис" }];
const EMPLOYMENT_OPTIONS: Array<{ value: EmploymentTypeFilter; label: string }> = [{ value: "any", label: "Любая занятость" }, { value: "fullTime", label: "Полная" }, { value: "partTime", label: "Частичная" }, { value: "contract", label: "Контракт / проект" }, { value: "internship", label: "Стажировка" }];

const EXPERIENCE_OPTIONS: Array<{ value: ExperienceFilter; label: string }> = [
  { value: "any", label: "Любой опыт" }, { value: "noExperience", label: "Без опыта" },
  { value: "between1And3", label: "1–3 года" }, { value: "between3And6", label: "3–6 лет" }, { value: "moreThan6", label: "Более 6 лет" },
];

function sourceLabel(source: string): string { if (source === "ats") return "ATS работодателей"; return JOB_SOURCES[source]?.label || source; }
function formatRefreshTime(timestamp: number): string { return timestamp ? new Intl.DateTimeFormat("ru-RU", { day: "2-digit", month: "2-digit", hour: "2-digit", minute: "2-digit" }).format(new Date(timestamp)) : "неизвестно"; }
function RefreshStatus({ source, meta }: { source: RealJobSource; meta: SourceRefreshMeta }) { return <div className="rounded-lg border border-border bg-muted/30 px-3 py-2 text-[11px] font-mono text-muted-foreground"><div className="flex items-center gap-2 flex-wrap"><span className="font-semibold text-foreground">{sourceLabel(source)}</span>{meta.stale && <span className="text-amber-400">показан сохранённый кэш</span>}</div><div className="mt-1">Обновлено: {formatRefreshTime(meta.lastUpdated)} · следующее обновление: {formatRefreshTime(meta.nextRefresh)}</div></div>; }

function SearchResultCard({ result, favorite, onToggleFavorite }: { result: SearchResult; favorite: boolean; onToggleFavorite: () => void }) {
  const source = JOB_SOURCES[result.source] || JOB_SOURCES.hh;
  return <article className="rounded-[1.25rem] border border-border bg-card p-5 hover:border-[var(--neon-violet)]/30 transition-all shadow-[var(--shadow-soft)] hover:-translate-y-0.5">
    <div className="flex items-start justify-between gap-3"><div className="min-w-0 flex-1">
      <div className="flex items-center gap-2 flex-wrap mb-1"><span className={`text-[10px] font-mono px-2 py-0.5 rounded-full border ${source.color} ${source.bg} ${source.border}`}>{source.label}</span><span className="text-[11px] font-mono text-muted-foreground flex items-center gap-1"><Clock size={10} />{result.publishedAt}</span></div>
      <h3 className="font-semibold text-base leading-snug mb-1">{result.title}</h3><div className="text-sm text-[var(--neon-violet)] flex items-center gap-1.5 mb-2"><Building2 size={13} />{result.company}</div>
      <div className="flex flex-wrap gap-x-4 gap-y-1 text-xs font-mono text-muted-foreground"><span className="flex items-center gap-1"><MapPin size={11} />{result.location}</span><span>{result.experience}</span><span className="text-[var(--success)]">{result.salary}</span></div>
    </div><button type="button" onClick={onToggleFavorite} className={`p-2 rounded-lg border transition-colors shrink-0 ${favorite ? "border-amber-400/40 text-amber-400 bg-amber-400/10" : "border-border text-muted-foreground hover:text-amber-400"}`} aria-label={favorite ? "Удалить из избранного" : "Добавить в избранное"}><Star size={15} fill={favorite ? "currentColor" : "none"} /></button></div>
    {result.tags.length > 0 && <div className="flex flex-wrap gap-1.5 mt-3">{result.tags.map((tag) => <span key={tag} className="px-2 py-0.5 rounded-md bg-muted text-[10px] font-mono text-muted-foreground">{tag}</span>)}</div>}
    <div className="mt-4 pt-3 border-t border-border/70 flex justify-end"><a href={result.url} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-2 px-3 py-2 rounded-lg border border-[var(--neon-cyan)]/30 text-xs font-mono text-[var(--neon-cyan)] hover:bg-[var(--neon-cyan)]/8" aria-label={`Открыть вакансию ${result.title}`}>Открыть вакансию <ExternalLink size={12} /></a></div>
  </article>;
}

export function SearchPanel({ config }: { config: Config }) {
  const [query, setQuery] = useState(config.jobTitle || ""); const [workMode, setWorkMode] = useState<WorkModeFilter>(config.workMode || "any"); const [location, setLocation] = useState(config.location || ""); const [employmentType, setEmploymentType] = useState<EmploymentTypeFilter>(config.employmentType || "any"); const [salaryFrom, setSalaryFrom] = useState(config.salaryFrom || ""); const [salaryTo, setSalaryTo] = useState(config.salaryTo || ""); const [salaryCurrency, setSalaryCurrency] = useState<SalaryCurrency>(config.salaryCurrency || "RUB"); const [areaId, setAreaId] = useState(config.areaId || "1"); const [experience, setExperience] = useState<ExperienceFilter>(config.experience || "any");
  const [activeSources, setActiveSources] = useState<Set<RealJobSource>>(new Set(USER_SOURCES)); const [response, setResponse] = useState<SearchResponse>({ results: [], errors: {}, nextHhPage: null }); const [lastRequest, setLastRequest] = useState<SearchRequest | null>(null);
  const [favorites, setFavorites] = useState<SearchResult[]>(loadFavorites); const [history, setHistory] = useState<SearchHistoryEntry[]>(loadSearchHistory); const [showFavorites, setShowFavorites] = useState(false); const [loading, setLoading] = useState(false); const [loadingMore, setLoadingMore] = useState(false); const [searched, setSearched] = useState(false);
  const favoriteIds = new Set(favorites.map((item) => item.id));
  const toggleSource = (source: RealJobSource) => setActiveSources((current) => { const next = new Set(current); if (next.has(source)) { if (next.size > 1) next.delete(source); } else next.add(source); return next; });
  const toggleFavorite = (result: SearchResult) => setFavorites((current) => { const next = current.some((item) => item.id === result.id) ? current.filter((item) => item.id !== result.id) : [result, ...current]; persistFavorites(next); return next; });

  const runSearch = async ({ searchQuery, searchAreaId, searchSalaryFrom, searchSalaryTo, searchSalaryCurrency, searchWorkMode, searchLocation, searchEmploymentType, searchExperience, sources }: { searchQuery: string; searchAreaId: string; searchSalaryFrom: string; searchSalaryTo: string; searchSalaryCurrency: SalaryCurrency; searchWorkMode: WorkModeFilter; searchLocation: string; searchEmploymentType: EmploymentTypeFilter; searchExperience: ExperienceFilter; sources: RealJobSource[] }) => {
    const trimmedQuery = searchQuery.trim(); if (!trimmedQuery || loading || loadingMore) return;
    const request: SearchRequest = { query: trimmedQuery, areaId: searchAreaId, salaryFrom: searchSalaryFrom, salaryTo: searchSalaryTo, salaryCurrency: searchSalaryCurrency, workMode: searchWorkMode, location: searchLocation, employmentType: searchEmploymentType, experience: searchExperience, telegramChannels: config.telegramChannels, sources, page: 0 };
    setLoading(true); setSearched(false); setShowFavorites(false); setResponse({ results: [], errors: {}, nextHhPage: null }); setLastRequest(request);
    try { const next = await searchJobs(request); setResponse(next); setHistory(addSearchHistory({ query: trimmedQuery, areaId: searchAreaId, salaryFrom: searchSalaryFrom, salaryTo: searchSalaryTo, salaryCurrency: searchSalaryCurrency, workMode: searchWorkMode, location: searchLocation, employmentType: searchEmploymentType, experience: searchExperience, sources })); } finally { setLoading(false); setSearched(true); }
  };
  const handleSearch = () => runSearch({ searchQuery: query, searchAreaId: areaId, searchSalaryFrom: salaryFrom, searchSalaryTo: salaryTo, searchSalaryCurrency: salaryCurrency, searchWorkMode: workMode, searchLocation: location, searchEmploymentType: employmentType, searchExperience: experience, sources: Array.from(activeSources) });
  const loadMore = async () => { if (!lastRequest || response.nextHhPage === null || loading || loadingMore) return; setLoadingMore(true); try { const next = await searchJobs({ ...lastRequest, sources: ["hh"], page: response.nextHhPage }); setResponse((current) => ({ results: mergeSearchResults(current.results, next.results), errors: { ...current.errors, ...next.errors }, nextHhPage: next.nextHhPage, refresh: current.refresh })); } finally { setLoadingMore(false); } };
  const repeatSearch = (entry: SearchHistoryEntry) => { setQuery(entry.query); setAreaId(entry.areaId); setSalaryFrom(entry.salaryFrom); setSalaryTo(entry.salaryTo || ""); setSalaryCurrency(entry.salaryCurrency || "RUB"); setWorkMode(entry.workMode || "any"); setLocation(entry.location || ""); setEmploymentType(entry.employmentType || "any"); setExperience(entry.experience || "any"); setActiveSources(new Set(entry.sources)); void runSearch({ searchQuery: entry.query, searchAreaId: entry.areaId, searchSalaryFrom: entry.salaryFrom, searchSalaryTo: entry.salaryTo || "", searchSalaryCurrency: entry.salaryCurrency || "RUB", searchWorkMode: entry.workMode || "any", searchLocation: entry.location || "", searchEmploymentType: entry.employmentType || "any", searchExperience: entry.experience || "any", sources: entry.sources }); };
  const clearHistory = () => { clearSearchHistory(); setHistory([]); };
  const failedSources = Object.entries(response.errors); const allRequestedSourcesFailed = Boolean(lastRequest) && lastRequest.sources.every((source) => Boolean(response.errors[source])); const visibleResults = showFavorites ? favorites : response.results;
  const limitedRefresh = LIMITED_REFRESH_SOURCES.map((source) => [source, response.refresh?.[source]] as const).filter((entry): entry is readonly [RealJobSource, SourceRefreshMeta] => Boolean(entry[1]));

  return <div className="space-y-6">
    <section className="overflow-hidden rounded-[1.5rem] border border-border bg-card p-6 sm:p-7 shadow-[var(--shadow-card)]">
      <div className="grid lg:grid-cols-[1fr_230px] gap-6 items-center">
        <div>
          <div className="inline-flex items-center gap-2 rounded-full border border-primary/20 bg-secondary px-3 py-1 text-xs font-medium text-secondary-foreground mb-4"><span className="h-2 w-2 rounded-full bg-[var(--success)]" />Career search</div>
          <h1 className="text-2xl sm:text-3xl font-semibold tracking-tight mb-2 flex items-center gap-2"><Target size={22} className="text-[var(--neon-violet)]" />Найти работу</h1>
          <p className="text-sm text-muted-foreground max-w-2xl leading-6 mb-5">Собирайте вакансии из нескольких источников в одном спокойном рабочем пространстве — без лишнего шума и дублирования.</p>
        </div>
        <div className="hidden lg:block relative h-36 rounded-[1.25rem] bg-gradient-to-br from-[#eef0ff] via-[#f5f2ff] to-[#eaf5ff] overflow-hidden">
          <div className="absolute left-8 bottom-5 h-20 w-14 rounded-[45%] bg-[#8b8fe8]" /><div className="absolute left-4 bottom-2 h-10 w-22 rounded-full bg-[#dcdcff]" />
          <div className="absolute left-10 top-7 h-10 w-10 rounded-full bg-[#f1c7ad] border-4 border-white/80" />
          <div className="absolute right-7 top-7 h-16 w-16 rounded-[1.1rem] bg-white/80 rotate-6 shadow-sm"><div className="m-3 h-2 rounded-full bg-[#b9bdf5]" /><div className="m-3 mt-2 h-2 w-8 rounded-full bg-[#e2e4f5]" /></div>
          <div className="absolute right-10 bottom-4 h-3 w-20 rounded-full bg-[#c8d7f4]" />
        </div>
      </div>
      <div className="space-y-4"><div className="flex flex-col sm:flex-row gap-2"><div className="flex-1 relative"><Briefcase size={13} className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" /><input value={query} onChange={(e) => setQuery(e.target.value)} onKeyDown={(e) => e.key === "Enter" && void handleSearch()} list="profession-examples" placeholder="QA-инженер, дизайнер, разработчик…" autoFocus className="w-full pl-9 pr-3 py-2.5 rounded-xl border border-border bg-input-background text-sm font-mono text-foreground" /><datalist id="profession-examples">{PROFESSION_EXAMPLES.map((p) => <option key={p} value={p} />)}</datalist></div><button onClick={() => void handleSearch()} disabled={loading || loadingMore || !query.trim()} className="flex items-center justify-center gap-2 px-5 py-2.5 rounded-xl font-semibold text-sm text-white disabled:opacity-40 min-h-[44px]" style={{ background: "linear-gradient(135deg, #8B5CF6, #7c3aed)" }}>{loading ? <Loader2 size={14} className="animate-spin" /> : <Target size={14} />}{loading ? "Ищу…" : "Найти"}</button></div>
      <div className="space-y-1.5"><span className="text-[10px] font-mono uppercase tracking-wider text-muted-foreground">Быстрый выбор</span><div className="flex gap-1.5 flex-wrap">{PROFESSION_GROUPS.map((group) => <button key={group.label} type="button" onClick={() => setQuery(group.examples[0])} title={group.examples.join(", ")} className="px-2.5 py-1.5 rounded-lg border border-border text-[11px] font-mono text-muted-foreground">{group.label}</button>)}</div></div>
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3"><label className="space-y-1"><span className="text-[10px] font-mono uppercase text-muted-foreground">Формат работы</span><select value={workMode} onChange={(e) => setWorkMode(e.target.value as WorkModeFilter)} className="w-full px-3 py-2.5 rounded-xl border border-border bg-input-background text-sm font-mono">{WORK_MODE_OPTIONS.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="space-y-1"><span className="text-[10px] font-mono uppercase text-muted-foreground">Локация</span><input value={location} onChange={(e) => setLocation(e.target.value)} placeholder="Москва, London…" className="w-full px-3 py-2.5 rounded-xl border border-border bg-input-background text-sm font-mono" /></label><label className="space-y-1"><span className="text-[10px] font-mono uppercase text-muted-foreground">Тип занятости</span><select value={employmentType} onChange={(e) => setEmploymentType(e.target.value as EmploymentTypeFilter)} className="w-full px-3 py-2.5 rounded-xl border border-border bg-input-background text-sm font-mono">{EMPLOYMENT_OPTIONS.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><label className="space-y-1"><span className="text-[10px] font-mono uppercase text-muted-foreground">Регион</span><select value={areaId} onChange={(e) => setAreaId(e.target.value)} className="w-full px-3 py-2.5 rounded-xl border border-border bg-input-background text-sm font-mono">{AREA_OPTIONS.map((area) => <option key={area.id} value={area.id}>{area.name}</option>)}</select></label><label className="space-y-1"><span className="text-[10px] font-mono uppercase text-muted-foreground">Зарплата от</span><input type="number" min="0" value={salaryFrom} onChange={(e) => setSalaryFrom(e.target.value)} placeholder="200000" className="w-full px-3 py-2.5 rounded-xl border border-border bg-input-background text-sm font-mono" /></label><label className="space-y-1"><span className="text-[10px] font-mono uppercase text-muted-foreground">Зарплата до</span><input type="number" min="0" value={salaryTo} onChange={(e) => setSalaryTo(e.target.value)} placeholder="300000" className="w-full px-3 py-2.5 rounded-xl border border-border bg-input-background text-sm font-mono" /></label><label className="space-y-1"><span className="text-[10px] font-mono uppercase text-muted-foreground">Валюта</span><select value={salaryCurrency} onChange={(e) => setSalaryCurrency(e.target.value as SalaryCurrency)} className="w-full px-3 py-2.5 rounded-xl border border-border bg-input-background text-sm font-mono">{SALARY_CURRENCIES.map((currency) => <option key={currency.value} value={currency.value}>{currency.label}</option>)}</select></label></div><div className="grid grid-cols-1 sm:grid-cols-2 gap-3"><label className="space-y-1"><span className="text-[10px] font-mono uppercase text-muted-foreground">Опыт</span><select value={experience} onChange={(e) => setExperience(e.target.value as ExperienceFilter)} className="w-full px-3 py-2.5 rounded-xl border border-border bg-input-background text-sm font-mono">{EXPERIENCE_OPTIONS.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label><div className="flex items-end"><p className="text-[11px] text-muted-foreground leading-5">Фильтр зарплаты учитывает диапазон и валюту вакансии. Вакансии без указанной зарплаты при активном фильтре не показываются.</p></div></div>
      <div className="space-y-1.5"><span className="text-[10px] font-mono uppercase text-muted-foreground">Основные источники</span><div className="flex gap-2 flex-wrap">{USER_SOURCES.map((source) => { const meta = JOB_SOURCES[source]; const selected = activeSources.has(source); return <button key={source} type="button" onClick={() => toggleSource(source)} className={`flex items-center gap-1.5 px-3 py-2 rounded-lg text-xs font-mono border ${selected ? `${meta.color} ${meta.bg} ${meta.border}` : "text-muted-foreground border-border"}`}>{selected && <Check size={11} />}{meta.label}</button>; })}</div><p className="text-[10px] font-mono text-muted-foreground">Feeds, ATS и {config.telegramChannels.length ? `${config.telegramChannels.length} Telegram-канал(а/ов)` : "Telegram после добавления каналов в настройках"} подключаются автоматически.</p></div></div>
    </section>

    {(history.length > 0 || favorites.length > 0) && <section className="rounded-[1.25rem] border border-border bg-card p-4 sm:p-5 space-y-3 shadow-[var(--shadow-soft)]"><div className="flex items-center justify-between gap-3 flex-wrap"><div className="flex gap-2"><button type="button" onClick={() => setShowFavorites(false)} className={`px-3 py-1.5 rounded-lg border text-xs font-mono ${!showFavorites ? "text-[var(--neon-violet)]" : "text-muted-foreground"}`}>Последние запросы</button><button type="button" onClick={() => setShowFavorites(true)} className={`px-3 py-1.5 rounded-lg border text-xs font-mono ${showFavorites ? "text-amber-400" : "text-muted-foreground"}`}><Star size={11} className="inline mr-1" />Избранное · {favorites.length}</button></div>{!showFavorites && history.length > 0 && <button type="button" onClick={clearHistory} className="text-xs font-mono text-muted-foreground hover:text-red-400"><Trash2 size={11} className="inline mr-1" />Очистить историю</button>}</div>{!showFavorites && <div className="flex flex-wrap gap-2">{history.map((entry) => <button key={entry.id} type="button" onClick={() => repeatSearch(entry)} className="px-3 py-2 rounded-lg border border-border text-xs font-mono"><span>{entry.query}</span><span className="text-muted-foreground"> · {AREA_OPTIONS.find((area) => area.id === entry.areaId)?.name || "Регион"}</span></button>)}</div>}</section>}

    <AnimatePresence mode="wait">{loading && <motion.div key="loading" initial={{ opacity: 0 }} animate={{ opacity: 1 }} className="py-12 text-center"><Loader2 size={28} className="animate-spin text-[var(--neon-violet)] mx-auto mb-3" /><p className="text-sm font-mono text-muted-foreground">Получаю вакансии из нескольких независимых источников…</p></motion.div>}
    {!loading && showFavorites && <motion.div key="favorites" initial={{ opacity: 0 }} animate={{ opacity: 1 }}><div className="text-sm font-mono text-muted-foreground mb-3">Избранных вакансий: <span className="text-foreground font-semibold">{favorites.length}</span></div>{favorites.length === 0 ? <div className="rounded-xl border border-border bg-card py-10 text-center text-sm font-mono text-muted-foreground">Добавляйте вакансии звездой — они сохраняются в этом браузере.</div> : <div className="space-y-2">{favorites.map((result) => <SearchResultCard key={result.id} result={result} favorite onToggleFavorite={() => toggleFavorite(result)} />)}</div>}</motion.div>}
    {searched && !loading && !showFavorites && <motion.div key="results" initial={{ opacity: 0 }} animate={{ opacity: 1 }}><div className="flex items-center justify-between gap-3 mb-3"><div className="text-sm font-mono text-muted-foreground">Найдено <span className="font-semibold text-foreground">{visibleResults.length}</span> вакансий</div><button onClick={() => void handleSearch()} disabled={loadingMore} className="flex items-center gap-1.5 px-3 py-1.5 rounded-lg border border-border text-xs font-mono text-muted-foreground"><RefreshCw size={11} />Обновить поиск</button></div>
      {limitedRefresh.length > 0 && <div className="grid grid-cols-1 md:grid-cols-2 gap-2 mb-3">{limitedRefresh.map(([source, meta]) => <RefreshStatus key={source} source={source} meta={meta} />)}</div>}
      {failedSources.length > 0 && <div className="space-y-2 mb-3">{failedSources.map(([source, message]) => <div key={source} className="flex items-start gap-2 rounded-lg border border-amber-400/25 bg-amber-400/8 px-3 py-2 text-xs font-mono text-amber-700"><AlertCircle size={12} className="mt-0.5" />{sourceLabel(source)}: {message}. Результаты других источников сохранены.</div>)}</div>}
      {allRequestedSourcesFailed && visibleResults.length === 0 ? <div className="rounded-xl border border-red-400/25 bg-red-400/5 py-10 px-4 text-center text-sm font-mono text-red-700">Не удалось получить вакансии из доступных источников.</div> : visibleResults.length === 0 ? <div className="rounded-xl border border-border bg-card py-12 px-4 text-center text-sm font-mono text-muted-foreground">По этому запросу ничего не найдено. Попробуйте изменить профессию, формат работы, занятость, зарплату или локацию.</div> : <div className="space-y-2">{visibleResults.map((result) => <SearchResultCard key={result.id} result={result} favorite={favoriteIds.has(result.id)} onToggleFavorite={() => toggleFavorite(result)} />)}</div>}
      {response.nextHhPage !== null && visibleResults.length > 0 && lastRequest?.sources.includes("hh") && <div className="pt-4 flex justify-center"><button type="button" onClick={() => void loadMore()} disabled={loadingMore} className="inline-flex items-center gap-2 px-5 py-2.5 rounded-xl border border-[var(--neon-violet)]/35 text-sm font-mono text-[var(--neon-violet)]">{loadingMore && <Loader2 size={13} className="animate-spin" />}{loadingMore ? "Загружаю…" : "Загрузить ещё с HH.ru"}</button></div>}
    </motion.div>}</AnimatePresence>

    <section className="rounded-[1.25rem] border border-border bg-card p-4 sm:p-5 shadow-[var(--shadow-soft)]"><h2 className="text-xs font-mono uppercase tracking-wider text-muted-foreground mb-3">Дополнительные площадки</h2><div className="flex flex-wrap gap-2">{EXTERNAL_SOURCES.map((source) => { const meta = JOB_SOURCES[source]; const url = meta.buildSearchUrl(query.trim() || config.jobTitle || "работа", areaId, salaryFrom); return <a key={source} href={url} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1.5 px-3 py-2 rounded-lg border border-border text-xs font-mono text-muted-foreground hover:text-foreground">{meta.label}<ExternalLink size={10} /></a>; })}</div></section>
  </div>;
}
